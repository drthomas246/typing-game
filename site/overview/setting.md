---
title: 設定
---
- `src/lib/texts/qa_test.ts` を基に、質問と翻訳を含むファイルを作成します。
- 上記で作成したファイルを App.tsx にインポートし、ボタンを配置します。
- `useSpeech` フックを使用して音声設定（言語コード、速度、音声品質）を変更します。
- UI のテーマと色は `src/styles` で変更できます。## 設定

- `src/lib/texts/qa_test.ts` を基に質問と翻訳を含むファイルを作成してください。
- 上記で作成したファイルを App.tsx にインポートし、ボタンを配置してください。
- `useSpeech` フックを使用して音声設定（言語コード、速度、音声品質）を変更してください。
- UI テーマと色は `src/styles` で変更できます。
